#!/bin/bash

# Detect if the buildpack should be applied
# Here, we just assume it should always be applied
# You might want to check for a specific file or environment variable

echo "elastic-apm-server"
exit 0

